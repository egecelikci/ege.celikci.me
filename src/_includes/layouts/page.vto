---
layout: layouts/base.vto
---

<article>
  {{ if title }}
    {{ comp.ui.pageHeader({
      title: title,
      subtitle: subtitle,
      centered: true,
      width: "w-full"
    }) }}
  {{ /if }}

  {{ comp ui.container { 
      classes: "pb-20 markdown text-lg leading-relaxed" 
  } }}
      {{ content |> safe }}
  {{ /comp }}
</article>