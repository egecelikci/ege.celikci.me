---
layout: layouts/base.vto
title: music
description: "a collection of favorite music albums."
url: /music/
menu:
  visible: true
  order: 2
templateEngine: [vto]
---

{{ set intro }}
  automatically fetched from my public <a
    href="https://critiquebrainz.org/user/{{ author.username }}"
  >
    album ratings</a>.
{{ /set }}

{{# --- HEADER --- #}}
{{
  comp.ui.PageHeader({
    title: title,
    subtitle: intro,
    centered: true,
  })
}}

{{# --- ALBUM GRID --- #}}
{{ set gridContent }}
  <div
    class="grid grid-cols-2 md:grid-cols-[repeat(auto-fill,minmax(180px,1fr))] gap-4 md:gap-8 lg:gap-10 py-8"
  >
    {{ for i, album of favorite.albums |> shuffle }}
      {{
        comp.ui.Album({
          album: album,
          priority: i < 6,
        })
      }}
    {{ /for }}
  </div>
{{ /set }}

{{# --- CONTAINER --- #}}
{{
  comp.ui.Container({
    width: "default",
    classes: "pb-20",
    content: gridContent,
  })
}}
