<!DOCTYPE html>
<html lang="{{ meta.lang }}">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ meta.title }}</title>

    {% if not build.dev and not build.naked %}
      {% viteLinkStylesheetTags %}
    {% endif %}
    {% include "meta.njk" %}
  </head>
  <body>
    <a href="#main" class="sr-skip-link">skip to main content</a>

    <div class="layout" role="document">
      {% include "header.njk" %}
      <main id="main" class="main" tabindex="-1">{{ content | safe }}</main>
      {% include "footer.njk" %}
    </div>

    {% if build.dev %}
      <script type="module" src="http://localhost:5173/@vite/client"></script>
      <script
        type="module"
        src="http://localhost:5173/src/assets/scripts/main.js"
      ></script>
    {% else %}
      {% viteScriptTag %}
    {% endif %}
  </body>
</html>
