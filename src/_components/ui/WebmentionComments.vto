{{#
  Component: WebmentionComments
  Minimalist display for interaction comments (replies, mentions).

  Props:
    - mentions: Array of filtered webmention objects (required)
    - classes: additional classes
#}}

<div class="webmentions-comments {{ it.classes }}">
  {{# Split mentions by type #}}
  {{
    set comments = it.mentions.filter((m) =>
      m["wm-property"] !== "like-of" && m["wm-property"] !== "repost-of"
    )
  }}

  {{# 2. COMMENTS LIST #}}
  {{ if comments.length > 0 }}
    <div class="webmentions-comments__list">
      <ol class="webmentions-comments__items">
        {{ for mention of comments }}
          <li class="webmentions-comments__item">
            {{ await it.comp.ui.WebmentionItem({ webmention: mention }) }}
          </li>
        {{ /for }}
      </ol>
    </div>
  {{ /if }}
</div>
