{{# 
  Uses the new Universal Badge + Clean Data 
#}}
{{ set config = wiki_status[status] || wiki_status.seedling }}

{{
  comp.ui.Badge({
    text: config.text,
    icon: config.icon,
    variant: config.variant,
  })
}}
