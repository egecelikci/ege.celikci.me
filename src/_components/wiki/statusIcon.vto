{{ set config = wiki_status[status] || wiki_status.seedling }}

<div
  class="flex items-center justify-center w-8 h-8 rounded-full border {{ config.border }} {{ config.bg }} {{ config.color }}"
  title="Status: {{ status }}"
>
  {{ comp.icon({ name: config.icon, type: "lucide", classes: "w-4 h-4" }) }}
</div>
