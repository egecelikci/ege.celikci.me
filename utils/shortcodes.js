import { Icon, renderTags, Callout } from "./components.js";

export const syncShortcodes = {
  icon: Icon,
  renderTags: renderTags,
};

export const pairedShortcodes = {
  callout: Callout,
};

export const asyncShortcodes = {};
